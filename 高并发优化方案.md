# 畅行租车 — 高并发优化方案（基于真实代码落地版）

> 本文档不再泛泛堆砌"微服务组件"，而是基于项目**当前真实代码**（`BookingService`、`CarService`、`TrimMapper`、`RabbitMQConfig` 等）逐条定位痛点、给出可直接落地的代码，并配套简历写法与面试话术。
>
> 每个场景按 **【现状痛点 → 怎么改 → 收益 → 简历写法 → 面试话术】** 组织，痛点均指向具体文件/行为。

---

## 0. 先纠正一个关键认知：租车库存 ≠ 电商计数库存

你原来的方案里写"Redis 预扣库存（借鉴点赞项目）"。点赞/秒杀是**电商计数型库存**：`stock:100` → `DECR` 一个数字就行。

但租车是**时间段占用型资源**：同一辆车在 `[6月15日, 6月18日]` 被租走后，任何与之日期重叠的订单都不能再租。这不是一个"剩余件数"能表达的，必须按**日期段占用**来做原子判断。

> 这个认知反过来就是面试加分点：能讲清"为什么租车不能简单 DECR 库存"的人，远少于会背"Redis 预扣库存"的人。

结合当前代码的实际语义：`checkCarAvailability`（`BookingService.java:327`）用 `trimId + pickupProvince + 日期重叠` 判断可用性，是一个**布尔占用模型**（有重叠订单就不可租）。所以下面优化的"库存单位"就是 `(trimId, province)` 的时间段占用，与当前代码完全对齐，而非虚构一个计数库存。

---

## 一、读多写少：首页车辆列表 / 车辆详情缓存

### 1.1 现状痛点

- `GET /cars` → `TrimController.carList` → `CarService.getHasStockCarList` → `TrimMapper.listHasStockTrim`（`TrimMapper.java:13`）。
- 每次请求都执行 `car_trims INNER JOIN cars INNER JOIN stores` 的三表关联 + `ORDER BY`，**无任何缓存**。
- `GET /cars/hot`（热门车型，`listHotTrims`，`TrimMapper.java:154`）还带 `LEFT JOIN bookings` 的 `COUNT` 聚合，首页高频访问下是典型的数据库热点。

### 1.2 怎么改

**两级缓存：Caffeine（进程内）+ Redis（共享），Cache Aside 一致性策略。**

① 引入 Caffeine 本地缓存 + Redis 序列化支持（`changxing-car/pom.xml` 增加依赖）：

```xml
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-data-redis</artifactId>
</dependency>
<dependency>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-cache</artifactId>
</dependency>
<dependency>
    <groupId>com.github.ben-manes.caffeine</groupId>
    <artifactId>caffeine</artifactId>
</dependency>
```

② 配置两级缓存（本地 1 分钟 / Redis 5 分钟，随机抖动防雪崩）：

```java
@Configuration
@EnableCaching
public class CacheConfig {

    @Bean
    public CacheManager cacheManager(RedisConnectionFactory factory) {
        // 本地 Caffeine：容量 1000，TTL 60s
        CaffeineCacheManager caffeine = new CaffeineCacheManager("carList", "carDetail");
        caffeine.setCaffeine(Caffeine.newBuilder()
                .maximumSize(1000)
                .expireAfterWrite(60, TimeUnit.SECONDS));

        // Redis：TTL 5 分钟 + 随机抖动
        RedisCacheConfiguration redisConf = RedisCacheConfiguration.defaultCacheConfig()
                .entryTtl(Duration.ofMinutes(5))
                .serializeValuesWith(RedisSerializationContext.SerializationPair
                        .fromSerializer(new GenericJackson2JsonRedisSerializer()));

        return new CompositeCacheManager(caffeine, 
                RedisCacheManager.builder(factory).cacheDefaults(redisConf).build());
    }
}
```

③ 列表查询加缓存注解（`CarService.getHasStockCarList`），并做**空值缓存 + 随机过期**防穿透：

```java
// key: car:list:{type}:{province}:{keyword}
@Cacheable(value = "carList",
        key = "T(com.changxing.common.constant.CacheKeys).listKey(#type,#province,#keyword)",
        unless = "#result == null || #result.isEmpty()")
public List<CarDTO> getHasStockCarList(String type, String province, String keyword) {
    List<CarDTO> list = trimMapper.listHasStockTrim(type, province, keyword);
    // 空结果也缓存 30 秒，防止缓存穿透打到 DB
    if (list == null || list.isEmpty()) {
        stringRedisTemplate.opsForValue()
                .set(CacheKeys.listKey(type, province, keyword), "[]", 30, TimeUnit.SECONDS);
    }
    return list;
}
```

④ **更新车辆/车款后删除缓存**（Cache Aside，先更库、后删缓存），在管理端 `AdminService` 或车辆服务的管理接口里统一调用：

```java
@CacheEvict(value = "carList", allEntries = true)
public void evictCarList() {}
// 或精确删除：@CacheEvict(value="carDetail", key="#trimId")
```

⑤ **缓存预热**：应用启动时预加载首页热门列表，避免冷启动穿透：

```java
@Component
@RequiredArgsConstructor
public class CarCacheWarmUp implements ApplicationRunner {
    private final CarService carService;
    @Override
    public void run(ApplicationArguments args) {
        carService.getHotTrims();          // 热门车型
        carService.getHasStockCarList(null, null, null); // 首页默认列表
    }
}
```

### 1.3 收益

- 首页列表 QPS 从"每次 3 表 JOIN"降到本地缓存命中（微秒级），数据库读压力大幅下降。
- Redis 命中承担集群共享，本地 Caffeine 再砍掉 Redis 网络 IO。

### 1.4 简历写法

> 针对车辆列表/详情等读多写少场景，落地 Caffeine 本地缓存 + Redis 二级缓存；热门车型启动预热，采用 Cache Aside（先更库后删缓存）维持一致性，配合空值缓存与随机过期防穿透、防雪崩，显著降低 MySQL 查询压力。

### 1.5 面试话术

> "首页车辆列表原来是每次三表 JOIN 打 MySQL。我上了两级缓存：Caffeine 存进程内热点数据，Redis 做集群级共享，命中顺序是 本地→Redis→DB。更新车辆时遵循 Cache Aside：先更新数据库、再删缓存，保证最终一致；空结果也缓存 30 秒防穿透，过期时间加随机抖动防雪崩。"
>
> **追问"本地缓存多实例不一致怎么办？"** → 讲 trade-off：车辆基础信息变更不频繁，60 秒短暂不一致可接受；对强一致诉求的字段（如实时库存状态）不进本地缓存。要能说出"什么时候不用本地缓存"。

---

## 二、写请求削峰：下单 + 车辆占用（核心场景）

### 2.1 现状痛点（真实代码，`BookingService.java:47-185`）

1. **锁粒度错位**：锁 key 是 `booking:lock:car:{carId}`（`:53`），但可用性校验是 `trimId + province + 日期段`（`:63`、`:327`）。锁和校验不在同一维度：
   - 不同日期、甚至是同款同省的不同车，都会因为 `carId` 串在一起排队（吞吐低）。
   - 反过来，同款同省同日期、但 `carId` 不同的请求又能绕过锁（存在竞态隐患）。
2. **长事务**：`@Transactional` 包住了 Feign 远程调用 `carFeignClient.getCarById`（`:84`）和 MQ 发送（`:138`）——事务内做了远程调用和消息发送，DB 连接/行锁持有时间被拉长。
3. **日期重叠查询难走索引**：`checkCarAvailability` 用 `BETWEEN ... OR BETWEEN ... OR (<= AND >=)` 的重叠条件（`:331-339`），范围 + OR 组合很难命中索引，高并发下易退化为慢查询。

### 2.2 优化 1：Redis 原子日期段预占，替代"分布式锁排队"

用 Redis ZSET 维护 `(trimId, province)` 维度的日期占用集合，配合 Lua 原子完成"检查重叠 + 写入占用"，逻辑等价于现有 `selectCount>0`，但把判断从 MySQL 搬到 Redis，且不再需要全局分布式锁排队。

**Redis 结构：**

```
key: occ:trim:{trimId}:{province}
类型: ZSET
score = 订单 endDate 的 epochDay
member = "startEpochDay~endEpochDay"    // 例如 "20627~20630"
```

**Lua 原子预占脚本（`lua/occupy.lua`）：**

```lua
-- KEYS[1] = occ:trim:{trimId}:{province}
-- ARGV[1]=startEpochDay  ARGV[2]=endEpochDay  ARGV[3]="start~end"
local start  = tonumber(ARGV[1])
local finish = tonumber(ARGV[2])
-- 取出所有 end >= start 的记录（ZSET 的 score 即 end），只需再判 start' <= finish
local members = redis.call('ZRANGEBYSCORE', KEYS[1], start, '+inf')
for _, m in ipairs(members) do
    local s, e = m:match("(%d+)~(%d+)")
    if tonumber(s) <= finish then   -- e>=start 已由 range 保证，只剩 start'<=finish 需判
        return 0                    -- 有重叠，预占失败
    end
end
redis.call('ZADD', KEYS[1], finish, ARGV[3])
return 1
```

**下单服务改造（伪代码，替换原 `tryLock` 串行逻辑）：**

```java
public BookingDTO createBooking(BookingCreateRequest request, Long userId) {
    validateRequest(request);
    Long trimId = request.getTrimId() != null ? request.getTrimId() : request.getCarId();
    String province = request.getPickupProvince();

    // 1. Redis 原子预占（不再用全局分布式锁排队）
    if (!occupancyService.tryOccupy(trimId, province,
            request.getStartDate(), request.getEndDate())) {
        throw new BusinessException("该车辆在该时间段已被预订"); // 快速失败
    }

    // 2. 生成订单，发 MQ 异步落库（事务只包 DB 写，见 3.2）
    BookingCreateMessage msg = buildCreateMessage(request, userId, trimId);
    try {
        // 本地事务内只做：insert booking
        Booking booking = bookingPersistService.createPending(request, userId, trimId);
        // 事务提交后，再发 MQ（事务外）
    } catch (Exception e) {
        occupancyService.release(trimId, province, request.getStartDate(), request.getEndDate());
        throw e;
    }
    messageProducerService.sendBookingCreateMessage(msg); // 事务外发送
    return convertToDTO(booking);
}
```

**超时未支付释放 + 消息兜底（定时补偿）：**

```java
@Component
public class OccupancyReconcileJob {
    // 1) 超时未支付释放：扫描 bookings 中 status=pending 且超过 15 分钟未支付的订单
    //    释放 occ:trim:{trimId}:{province} 中对应 member，并置 cancelled
    // 2) 对账：定时比对 Redis 占用集合 与 MySQL 活跃订单(日期重叠)
    //    不一致时以 MySQL 为准重建 Redis 占用集合（最终一致性兜底）
}
```

### 2.3 优化 2：MQ 异步落库（复用现有 RabbitMQ，补齐"削峰"语义）

当前 MQ 只做"下单成功后通知"（`MessageProducerService.sendBookingStatusMessage`），数据库写入仍是同步挡在请求线程上。改造为：

- 新增 `booking.create.queue`，预占成功后把"落库 + 通知车辆服务"作为异步任务；
- 请求线程只做"参数校验 + Redis 预占 + 生成订单号 + 快速返回受理结果"，把写 DB 的压力从请求线程剥离；
- 消费者做最终 insert（或直接 insert pending 后发状态消息），消费失败进死信队列重试。

> 注意要和已有 `booking.status.queue` 区分：`booking.status` 是"状态变更广播"，`booking.create` 是"下单写库削峰"，职责不同。

### 2.4 优化 3：锁粒度纠正（顺带修掉一致性隐患）

把原来 `booking:lock:car:{carId}` 的粗粒度全局锁语义，收敛为 `(trimId, province, 日期段)` 的占用语义：
- 不同车款、不同取车地天然并行，互不阻塞；
- 同一 `(trimId, province)` 的并发，由 Lua 的原子性保证，无需排队锁。

### 2.5 收益

- 下单接口从"排队拿锁 → 同步写库"变为"Redis 原子预占 → 快速失败/受理"，单位时间吞吐显著提升；DB 不再直接承受海量下单写请求。
- 修复了锁粒度与校验粒度错位导致的串行与竞态隐患。

### 2.6 简历写法

> 针对节假日抢车、热门车下单并发，将"车辆+取车地+日期段"的可用性判断下沉为 Redis 原子预占（Lua 检查日期段重叠），替代全局分布式锁排队；下单经 RabbitMQ 异步落库削峰，配合定时任务实现超时未释放与 Redis/MySQL 对账兜底，提升下单吞吐并消除超卖风险。

### 2.7 面试话术

> "原来的下单用 Redisson 全局锁按 carId 排队，高并发抢同一款车时所有请求串行等锁，吞吐低；而且事务里还包了 Feign 远程调用。我改成 Redis 原子预占：把每个'车款+取车地'的已占用日期段放进 ZSET，用一段 Lua 原子地'检查日期重叠→写入占用'，有重叠就快速失败返回'已被预订'，没有重叠才放行。放行后发 MQ 异步落库，写 DB 的压力从请求线程剥离。最后用定时任务处理超时未支付释放、以及对账兜底，保证 Redis 和 MySQL 最终一致。"
>
> **追问**："——为什么不用简单 DECR？" → 因为租车是按日期段占用的资源，不是剩余件数；一辆车 6/15~6/18 被租，任何日期重叠的订单都不能再租，必须按时间区间判断。
>
> **追问**："Redis 挂了 / 消息丢了怎么办？" → 数据库层保留唯一的最终防线（见第三节的唯一约束/乐观锁），定时对账用 MySQL 重建 Redis 占用，消息走 RabbitMQ 持久化 + 死信重试。

---

## 三、数据库层优化

### 3.1 索引优化

当前 `bookings` 只有 `user_id / car_id / status / (start_date,end_date)` 等单列或弱组合索引，但核心可用性查询是 `trim_id + status + 日期重叠`。补充：

```sql
-- 下单可用性检查核心索引（对齐 checkCarAvailability 的查询）
ALTER TABLE bookings
  ADD INDEX idx_trim_status_dates (trim_id, status, start_date, end_date);

-- 车辆列表库存判断
ALTER TABLE cars
  ADD INDEX idx_status_province_trim (status, province, trim_id);
```

### 3.2 缩短事务周期（对齐真实问题）

`createBooking` 的 `@Transactional` 目前包住了 Feign 调用和 MQ 发送。改造原则：

1. **事务只包 DB 写**：`insert booking` 单独一个本地事务；
2. **远程调用都在事务外**：`carFeignClient.getCarById` 提前到校验阶段；
3. **MQ 发送在事务提交后**：避免"DB 未提交、消息已消费"的顺序问题，也避免消息发送阻塞事务。

```java
// @Transactional 只落在持久化方法上，而不是整个 createBooking
@Transactional
public Booking createPending(BookingCreateRequest request, Long userId, Long trimId) {
    return bookingMapper.insert(buildBooking(request, userId, trimId));
}
// 发送 MQ 放在事务方法返回之后（TransactionSynchronization 或直接调用点调整）
```

### 3.3 冷热数据分离

`bookings` 预估 100 万+ 且持续增长（见数据库设计 7 章），把 `completed / cancelled` 的历史订单归档到 `bookings_archive`，主表只保留活跃订单，减小热点查询的扫表范围。

### 3.4 数据库防重兜底（最后一道防线）

作为超卖的最后防线，保留数据库级约束（对齐现有 `fix_bookings_fk.sql` 的补丁思路）：

```sql
-- 同一 trim + province 相同日期段，活跃订单唯一（配合应用层预占）
ALTER TABLE bookings ADD INDEX idx_trim_province_dates (trim_id, pickup_province, start_date, end_date);
```

### 3.5 收益 / 简历 / 话术

- **收益**：可用性查询命中复合索引；事务持有时长从"远程调用+写库"缩短到"仅写库"，行锁等待降低。
- **简历写法**：优化业务 SQL 建立 `(trim_id, status, start_date, end_date)` 复合索引；将事务范围收敛到仅 DB 写、移除事务内远程调用与消息发送，缩短行锁持有时间，降低高并发锁等待。
- **面试话术**："事务里做远程调用和发消息，会让这条连接和行锁被长时间占用。我把远程调用提前、消息发送放到事务提交后，事务只包数据库插入，锁持有时间降到最短。"

---

## 四、网关 + Sentinel 接口防护（深化）

### 4.1 现状痛点

全局只有 `feign.sentinel.enabled: true`（`application.yml`），**没有一条真正的限流/热点规则**；网关 `AuthFilter` 也只见鉴权、无流量防护。所谓"已接入 Sentinel"目前是空壳。

### 4.2 怎么改

① 网关层对下单接口做 **QPS 限流 + 热点参数限流**（对热门 `trimId` 单独限流）：

```java
// 下单接口：整体 QPS 限流（快速失败，不排队）
@PostMapping("/bookings")
@SentinelResource(value = "createBooking",
        blockHandler = "createBookingBlock",
        fallback = "createBookingFallback")
public Result<BookingDTO> createBooking(...) { ... }

public Result<BookingDTO> createBookingBlock(BookingCreateRequest req, HttpServletRequest httpReq, BlockException ex) {
    return Result.fail(429, "下单请求过多，请稍后重试");
}
```

② 热点参数规则（用 Sentinel 控制台/代码配规则，对频繁被抢的 `trimId` 限流）：

```
资源: createBooking
参数索引: 0（对应 trimId / carId 在参数列表中的位置）
热点规则: 单参数 QPS 阈值，例如 20
```

③ 非核心接口降级：AI 客服、积分更新等做降级，保障"下单 + 车辆查询"核心链路优先可用（`FallbackFactory` + 兜底返回）。

### 4.3 收益 / 简历 / 话术

- **收益**：抢热门车时，多余流量在网关/入口被快速拒绝，不会穿透到 Redis 预占和 DB；核心链路稳定。
- **简历写法**：基于 Sentinel 实现下单接口 QPS 限流与针对热门车款 ID 的热点参数限流，快速失败不排队；对非核心客服/积分接口做降级，保障下单与车辆查询核心链路。
- **面试话术**："热点参数限流解决的是'大家都在抢同一台车'的场景——对同一个 trimId 单独限 QPS，超出直接快速失败，避免把压力传导到库存预占和数据库。"

---

## 五、AI 客服并发优化

### 5.1 现状痛点

客服是 Python FastAPI Agent（`面试准备文档.md` 三章），直连 LLM（OpenAI/Ollama），每次问询都：意图识别 → 查后端 API → 拼 Prompt → 调 LLM。常见问题是：

1. 所有请求无差别打 LLM，LLM 调用延迟高、成本高、令牌配额有上限；
2. 相同/相似高频问题（"怎么取消订单""押金多久退"）重复调 LLM；
3. 会话历史每次全量拉取拼进 Prompt，长会话 token 膨胀、响应变慢。

### 5.2 怎么改

1. **高频问题缓存**：对意图识别结果 + 标准化答案做缓存（Redis/进程内），命中直接返回，不调 LLM；
2. **语义相似问复用**：对问句做向量/指纹相似度，命中近邻问题则复用已有回答，减少 LLM 调用；
3. **LLM 调用限流 + 异步队列**：对 LLM 出口做并发限制与排队（比如 `asyncio.Semaphore` 或独立限流器），避免高峰把 API Key 打爆；
4. **SSE 连接数限流**：限制单用户/全局 SSE 并发连接，防止恶意或突发长连接占满资源；
5. **会话上下文瘦身**：长会话做摘要压缩，或只取最近 N 条 + 摘要，控制 token。

### 5.3 收益 / 简历 / 话术

- **收益**：高频问题命中缓存后不再走 LLM，降低延迟与成本，峰值扛得住。
- **简历写法**：AI 客服通过高频问题缓存 + 相似问复用减少重复 LLM 调用，LLM 出口限流与异步队列削峰，SSE 连接限制与上下文摘要压缩，支撑高并发问答。
- **面试话术**："客服问答的瓶颈不在业务查询，在 LLM 调用（慢、贵、有配额）。我先做一层高频问题缓存，识别出标准问题直接返回；再对 LLM 出口做并发限制 + 队列，避免高峰打爆 API。长会话用摘要压缩控制 token，SSE 连接数也做了限制。"

---

## 六、收益汇总 & 面试高频追问

### 6.1 优化前后对照

| 场景 | 优化前（现状） | 优化后 | 核心收益 |
|------|----------------|--------|----------|
| 首页车辆列表 | 三表 JOIN 直打 MySQL，无缓存 | Caffeine + Redis 二级缓存 + 预热 | 读吞吐提升，DB 读压力下降 |
| 高峰期下单 | 全局锁按 carId 排队 + 同步写库 + 长事务 | Redis 原子日期段预占 + MQ 异步落库 + 短事务 | 下单吞吐提升，消除串行与超卖隐患 |
| 数据库热点行 | 日期重叠 OR 查询难走索引 + 长事务 | 复合索引 + 事务收敛到仅写库 | 行锁等待降低 |
| 接口防护 | Sentinel 空壳 | 热点参数限流 + QPS 限流 + 降级 | 核心链路稳定 |
| AI 客服 | 每次直连 LLM | 高频缓存 + 相似复用 + LLM 限流队列 | 延迟/成本下降，峰值可扛 |

> 说明：本文不虚构压测数字，建议你本地用 JMeter 对 `GET /cars` 和 `POST /bookings` 做优化前后对比，把真实 QPS/RT 落到简历里，比任何"提升 XX%"更有说服力。

### 6.2 高频追问清单（建议全部能脱稿回答）

1. 缓存穿透 / 雪崩 / 击穿分别怎么防？你的方案对应哪一段代码？
2. 写后删除缓存为什么不是"先删缓存再更库"？延迟双删了解吗？
3. Redis 预占和 MySQL 最终一致性怎么保证？对账任务怎么设计？
4. 下单消息丢失怎么办？（RabbitMQ 持久化 + 生产者确认 + 死信重试）
5. 为什么不用简单 DECR 库存而用日期段占用？
6. 热点参数限流的实现原理？（参数维度单独计数）
7. 本地缓存多实例一致性问题，你的取舍是什么？
8. LLM 调用怎么限流？高频问题缓存怎么设计失效？

---

## 附：涉及的真实文件清单（便于改动时对照）

| 文件 | 现状 | 改动 |
|------|------|------|
| `changxing-booking/.../service/BookingService.java` | 长事务 + carId 锁 + DB 重叠查询 | Redis 预占 + MQ 异步 + 短事务 |
| `changxing-booking/.../config/RabbitMQConfig.java` | 仅 status/completed 队列 | 新增 `booking.create.queue` |
| `changxing-booking/.../service/MessageProducerService.java` | 状态通知 | 新增下单削峰消息 |
| `changxing-car/.../service/CarService.java` | 无缓存列表查询 | 两级缓存 + 预热 + 删缓存 |
| `changxing-car/.../mapper/TrimMapper.java` | 三表 JOIN | 缓存削减查询频次 |
| `changxing-gateway/.../filter/AuthFilter.java` | 仅鉴权 | 叠加限流/热点参数 |
| `bookings` 表 | 弱索引 | 复合索引 + 归档表 |