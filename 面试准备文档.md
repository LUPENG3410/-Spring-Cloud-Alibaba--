# 畅行租车项目 - 面试准备文档

## 一、项目概述

### 1.1 项目简介
**项目名称**：畅行租车微服务系统 + 智能客服AI Agent

**项目周期**：2024年X月 - 2024年X月（根据实际情况填写）

**项目规模**：后端9个微服务 + 1个AI Agent服务 + Vue3前端

**技术栈**：
- **后端**：Spring Boot 3.2, Spring Cloud 2023, Spring Cloud Alibaba, Nacos, MySQL 8.0, Redis, RabbitMQ, MyBatis-Plus, Redisson, MinIO, Sentinel
- **AI Agent**：Python FastAPI, OpenAI SDK, SSE, Pydantic, httpx
- **前端**：Vue 3, TypeScript, Vite, Pinia, Vue Router, Axios, Three.js, Leaflet

### 1.2 项目架构
```
┌─────────────────────────────────────────────────────────────┐
│                      客户端层                                │
│   用户端 │ 管理端 │ H5端 │ App端 │ AI Agent                 │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                    API Gateway (8080)                        │
│            路由转发 │ 鉴权过滤 │ 限流熔断                    │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                  Spring Cloud 微服务集群                     │
│  用户服务 │ 车辆服务 │ 订单服务 │ 门店服务 │ 评价服务        │
│  认证服务 │ 管理服务 │ 客服服务 │ 上传服务                  │
└───────────────────────────┬─────────────────────────────────┘
                            │
┌───────────────────────────▼─────────────────────────────────┐
│                      中间件层                                │
│    MySQL │ Redis │ RabbitMQ │ MinIO │ Nacos │ Sentinel      │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、核心技术点详解及面试回答技巧

### 2.1 微服务架构设计

**面试回答技巧**：
```
"我在项目中采用了Spring Cloud微服务架构，将系统拆分为9个独立服务。
拆分原则是按业务领域划分：用户、车辆、订单、门店、评价等。
每个服务独立数据库，通过Nacos实现服务注册与配置管理，
使用Spring Cloud Gateway作为统一入口，实现路由、鉴权、限流。"
```

**核心要点**：
- **服务拆分**：按业务领域（DDD）拆分，每个服务职责单一
- **服务通信**：OpenFeign实现同步调用，RabbitMQ实现异步通信
- **服务治理**：Nacos服务注册/配置中心，Sentinel流量控制/熔断降级
- **数据一致性**：Seata分布式事务（如需要可展开讲）

**常见追问**：
- Q: 为什么选择微服务而不是单体？
  A: "业务复杂度高，多人协作开发，微服务可以独立部署、独立扩展，故障隔离。"
  
- Q: 微服务之间如何通信？
  A: "同步调用用OpenFeign，如查询车辆信息；异步通信用RabbitMQ，如订单状态变更通知。"

---

### 2.2 分布式锁（Redisson）

**面试回答技巧**：
```
"租车系统核心问题是高并发下同一辆车不能被多人同时下单。
我使用Redisson实现分布式锁，基于Redis的SETNX实现。
锁的粒度是车辆ID，确保同一时刻只有一个请求能创建该车的订单。
设置了5秒等待时间和30秒锁有效期，防止死锁。"
```

**代码实现要点**（BookingService.java:53-59）：
```java
String lockKey = BOOKING_LOCK_PREFIX + request.getCarId();
RLock lock = redissonClient.getLock(lockKey);
// 尝试获取锁，最多等待5秒，锁有效期30秒
if (!lock.tryLock(5, 30, TimeUnit.SECONDS)) {
    throw new BusinessException("系统繁忙，请稍后重试");
}
```

**核心要点**：
- **锁粒度**：按车辆ID加锁，细粒度控制并发
- **锁超时**：waitTime=5s（等待获取锁），leaseTime=30s（锁持有时间）
- **可重入锁**：Redisson支持可重入，同一线程可多次获取
- **自动续期**：Redisson的WatchDog机制自动续期，防止业务未完成锁已过期

**常见追问**：
- Q: 为什么不用MySQL悲观锁？
  A: "悲观锁性能差，高并发下会阻塞；分布式锁基于Redis，性能高，适合互联网场景。"
  
- Q: 锁超时了但业务还没完成怎么办？
  A: "Redisson有WatchDog机制，会自动续期。另外我在finally块中确保释放锁。"

---

### 2.3 JWT认证授权

**面试回答技巧**：
```
"认证采用JWT + Redis方案。用户登录后生成Access Token（24小时有效），
Token中包含用户ID、角色、会员等级等信息。
网关层统一鉴权，验证Token有效性，并将用户信息传递给下游服务。
Redis用于存储Token黑名单，支持主动注销。"
```

**代码实现要点**（JwtUtils.java）：
```java
// Token生成
public static String generateToken(String subject, Map<String, Object> claims) {
    return Jwts.builder()
        .subject(subject)
        .claims(claims)
        .issuedAt(new Date())
        .expiration(new Date(System.currentTimeMillis() + EXPIRE_MS))
        .signWith(getKey())
        .compact();
}

// Token验证
public static Claims parseToken(String token) {
    return Jwts.parser()
        .verifyWith(getKey())
        .build()
        .parseSignedClaims(token)
        .getPayload();
}
```

**核心要点**：
- **Token结构**：Header（算法）+ Payload（用户信息）+ Signature（签名）
- **双令牌**：Access Token（短期）+ Refresh Token（长期，用于刷新）
- **网关鉴权**：AuthFilter统一拦截，白名单放行，其余验证Token
- **用户信息传递**：通过Header传递X-User-Id、X-User-Role

**常见追问**:
- Q: JWT和Session的区别？
  A: "JWT是无状态的，服务端不存储会话信息，适合分布式系统；Session需要服务端存储，扩展性差。"
  
- Q: Token被盗了怎么办？
  A: "可以设置较短的过期时间，支持Token黑名单主动注销，敏感操作需要二次验证。"

---

### 2.4 API网关（Spring Cloud Gateway）

**面试回答技巧**：
```
"网关是系统的统一入口，我基于Spring Cloud Gateway实现。
主要职责：1)路由转发，根据路径分发到不同服务；2)统一鉴权，
在AuthFilter中验证JWT；3)跨域配置，支持多端访问；
4)限流熔断，集成Sentinel保护后端服务。"
```

**核心配置**：
```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: changxing-car
          uri: lb://changxing-car  # 负载均衡
          predicates:
            - Path=/api/cars/**
          filters:
            - StripPrefix=1
```

**核心要点**：
- **路由转发**：基于路径前缀分发到不同微服务
- **负载均衡**：lb://协议自动负载均衡
- **鉴权过滤**：GlobalFilter统一鉴权，白名单放行
- **跨域配置**：全局CORS配置，支持前端多端访问

---

### 2.5 OpenFeign服务调用

**面试回答技巧**：
```
"微服务之间通过OpenFeign进行同步调用。
比如订单服务需要查询车辆信息，通过CarFeignClient调用车辆服务。
OpenFeign集成了LoadBalancer，自动实现负载均衡和故障转移。"
```

**代码示例**：
```java
@FeignClient(name = "changxing-car", path = "/api")
public interface CarFeignClient {
    @GetMapping("/cars/{id}")
    Result<CarDTO> getCarDetail(@PathVariable("id") Long id);
}
```

**核心要点**：
- **声明式调用**：定义接口即可，框架自动生成实现
- **负载均衡**：集成Spring Cloud LoadBalancer
- **容错处理**：可集成Sentinel实现熔断降级
- **超时配置**：connect-timeout=5s, read-timeout=10s

---

### 2.6 消息队列（RabbitMQ）

**面试回答技巧**：
```
"跨服务异步通信使用RabbitMQ，实现了三个核心场景：
1. 订单状态变更通知：订单创建/确认/取消时，异步通知车辆服务更新车辆状态
2. 订单完成通知：订单完成时，异步通知用户服务更新积分和会员等级
3. 评价新增通知：用户评价时，异步通知车辆服务更新评分
使用Direct Exchange + 路由键精确分发，消息JSON序列化，支持重试和死信队列。"
```

**代码实现要点**：
```java
// 1. RabbitMQ配置类（订单服务）
@Configuration
public class RabbitMQConfig {
    public static final String BOOKING_EXCHANGE = "booking.exchange";
    public static final String BOOKING_STATUS_QUEUE = "booking.status.queue";
    public static final String BOOKING_COMPLETED_QUEUE = "booking.completed.queue";
    
    @Bean
    public DirectExchange bookingExchange() {
        return new DirectExchange(BOOKING_EXCHANGE, true, false);
    }
    
    @Bean
    public Queue bookingStatusQueue() {
        return QueueBuilder.durable(BOOKING_STATUS_QUEUE).build();
    }
}

// 2. 消息生产者（订单服务）
@Service
public class MessageProducerService {
    private final RabbitTemplate rabbitTemplate;
    
    public void sendBookingStatusMessage(Long bookingId, String orderNo, 
                                          Long userId, Long carId, String status) {
        MessageDTO message = MessageDTO.builder()
                .bookingId(bookingId)
                .orderNo(orderNo)
                .userId(userId)
                .carId(carId)
                .status(status)
                .timestamp(LocalDateTime.now())
                .build();
        rabbitTemplate.convertAndSend(BOOKING_EXCHANGE, "booking.status", message);
    }
}

// 3. 消息消费者（车辆服务）
@Component
public class BookingMessageConsumer {
    @RabbitListener(queues = "booking.status.queue")
    public void handleBookingStatusMessage(Map<String, Object> message) {
        Long trimId = Long.valueOf(message.get("carId").toString());
        String status = message.get("status").toString();
        String carStatus = mapBookingStatusToCarStatus(status);
        carService.updateCarStatus(trimId, carStatus);
    }
    
    private String mapBookingStatusToCarStatus(String bookingStatus) {
        switch (bookingStatus) {
            case "pending": case "confirmed": return "reserved";
            case "active": return "rented";
            case "completed": case "cancelled": return "available";
            default: return null;
        }
    }
}

// 4. 消息消费者（用户服务）
@Component
public class BookingMessageConsumer {
    @RabbitListener(queues = "booking.completed.queue")
    public void handleBookingCompletedMessage(Map<String, Object> message) {
        Long userId = Long.valueOf(message.get("userId").toString());
        Integer totalDays = Integer.valueOf(message.get("totalDays").toString());
        int points = totalDays * 10;  // 每天10积分
        userService.addUserPoints(userId, points);
        userService.updateMemberLevel(userId);
    }
}
```

**核心要点**：
- **解耦**：订单服务不直接调用车辆服务，通过消息解耦
- **异步**：非核心业务异步处理，提高响应速度
- **削峰**：高并发时缓冲请求，保护下游服务
- **可靠性**：消息持久化，确认机制，死信队列处理失败消息
- **精确分发**：Direct Exchange + 路由键，确保消息准确到达目标队列
- **状态映射**：订单状态到车辆状态的映射逻辑（pending→reserved, active→rented）

---

### 2.7 分布式事务（Seata）

**面试回答技巧**：
```
"跨服务数据一致性使用Seata解决。
比如创建订单时需要同时扣减车辆库存，这是跨服务操作。
Seata的AT模式可以保证两个操作要么都成功，要么都回滚。"
```

**核心要点**：
- **AT模式**：自动生成回滚SQL，对业务无侵入
- **全局事务**：通过XID传递事务上下文
- **补偿机制**：失败时自动回滚已执行的操作

---

### 2.8 缓存设计（Redis）

**面试回答技巧**：
```
"Redis主要用于：1)Token存储和黑名单；2)分布式锁；
3)热点数据缓存，如车辆列表、门店信息；4)会话管理。
采用String存储Token，Hash存储对象，Set存储排行榜。"
```

**核心要点**：
- **Token存储**：用户登录信息，支持快速查询
- **缓存穿透**：布隆过滤器或空值缓存
- **缓存雪崩**：随机过期时间，防止同时失效
- **缓存击穿**：分布式锁或永不过期+异步更新

---

### 2.9 文件存储（MinIO）

**面试回答技巧**：
```
"文件存储使用MinIO，兼容S3协议。
主要用于车辆图片、用户头像、门店图片等。
实现了图片上传、删除、访问功能，支持多类型目录管理。"
```

**核心要点**：
- **对象存储**：兼容AWS S3协议，支持海量文件
- **Bucket管理**：按业务分类存储，cars/avatar/store
- **文件校验**：类型校验、大小限制（5MB）
- **访问控制**：公开读取策略，支持CDN加速

---

### 2.10 流量控制（Sentinel）

**面试回答技巧**：
```
"使用Sentinel实现流量控制和熔断降级。
对核心接口设置QPS限制，防止突发流量击垮服务。
对不稳定服务（如调用第三方API）设置熔断，
失败率超过阈值时自动熔断，快速失败，避免雪崩。"
```

**核心要点**：
- **流量控制**：QPS限制、线程数限制、关联限流
- **熔断降级**：慢调用比例、异常比例、异常数
- **系统保护**：CPU、内存、负载自适应保护
- **热点参数**：对热点参数单独限流

---

## 三、AI Agent智能客服

### 3.1 架构设计

**面试回答技巧**：
```
"智能客服采用Agent架构，核心流程是：
用户消息 → 意图识别 → 数据查询 → Prompt构建 → LLM生成。
使用Python FastAPI实现，集成OpenAI兼容API，支持本地Ollama和云端GPT。"
```

**架构图**：
```
用户 → FastAPI → AgentService → DataService → 后端微服务API
                    ↓
              OpenAI/Ollama
                    ↓
              SSE流式响应 → 前端打字机效果
```

### 3.2 意图识别

**面试回答技巧**：
```
"意图识别采用规则引擎实现，支持10种意图类型。
通过关键词匹配和正则表达式，识别用户想查询什么数据。
比如用户说'朗逸多少钱'，会识别为car_detail意图，
提取车型关键词'朗逸'，然后调用后端API查询价格。"
```

**支持的意图类型**：
- search_cars：搜索车辆列表
- car_detail：查询车辆详情
- hot_cars：热门车型排行
- stores：门店查询
- user_bookings：用户订单
- user_profile：用户信息
- car_reviews：车辆评价

### 3.3 流式响应（SSE）

**面试回答技巧**：
```
"使用SSE实现流式响应，前端可实现打字机效果。
后端逐token返回LLM生成结果，前端实时渲染，
用户体验更好，不用等待完整回复。"
```

**SSE协议**：
```
event: start    → 开始生成
event: delta    → 文本增量
event: done     → 生成完毕
event: error    → 发生错误
```

### 3.4 多轮对话

**面试回答技巧**：
```
"通过conversation_id管理对话历史，支持多轮对话。
每次请求都会加载历史消息，作为LLM的上下文输入，
确保对话连贯。历史消息存储在后端客服服务中。"
```

### 3.5 数据注入（RAG）

**面试回答技巧**：
```
"为避免LLM幻觉，采用数据注入机制。
意图识别后，先查询真实业务数据，
将数据格式化后注入到system prompt中，
LLM基于真实数据回答，不会编造信息。"
```

---

## 四、常见面试问题及参考答案

### 4.1 项目相关

**Q: 项目中遇到的最大技术难点是什么？如何解决的？**

A: "高并发下车辆重复下单问题。解决方案是使用Redisson分布式锁，
按车辆ID加锁，确保同一时刻只有一个订单能创建。
同时优化了锁粒度和超时时间，平衡性能和安全性。"

**Q: 项目中有没有做性能优化？**

A: "有，主要三个方面：
1) Redis缓存热点数据，减少数据库查询；
2) RabbitMQ异步处理非核心业务，提高响应速度；
3) Sentinel限流熔断，保护系统稳定性。"

**Q: 项目部署架构是怎样的？**

A: "使用Docker Compose部署，每个服务独立容器。
Nacos作为注册中心，MySQL主从，Redis集群，
RabbitMQ集群，支持水平扩展。
各服务配置了RabbitMQ连接信息：
```yaml
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    virtual-host: /
```"

### 4.2 技术相关

**Q: Spring Cloud和Dubbo的区别？**

A: "Spring Cloud基于HTTP，生态丰富，适合互联网项目；
Dubbo基于TCP，性能高，适合内部服务调用。
我项目用Spring Cloud是因为生态完善，学习成本低。"

**Q: MyBatis和MyBatis-Plus的区别？**

A: "MyBatis-Plus是MyBatis的增强版，内置CRUD、分页、条件构造器，
开发效率更高。复杂SQL还是用MyBatis，简单查询用Plus。"

**Q: Redis有哪些数据类型？项目中怎么用的？**

A: "五种基本类型：String（Token存储）、Hash（对象存储）、
List（消息队列）、Set（排行榜）、ZSet（延迟队列）。
项目中主要用于Token存储、分布式锁、缓存。"

**Q: 如何保证分布式系统数据一致性？**

A: "根据场景选择方案：
强一致性用Seata分布式事务；
最终一致性用RabbitMQ消息队列+重试机制；
高并发用Redis缓存+数据库最终一致。"

**Q: RabbitMQ在项目中是怎么用的？**

A: "主要用在三个场景：
1. 订单状态变更时，异步通知车辆服务更新车辆状态（pending→reserved, active→rented）
2. 订单完成时，异步通知用户服务更新积分（每天10积分）和会员等级
3. 使用Direct Exchange + 路由键精确分发，消息JSON序列化
4. 消费者使用@RabbitListener监听队列，失败消息进入死信队列"

**Q: 为什么选择RabbitMQ而不是Kafka？**

A: "RabbitMQ适合业务解耦和异步通信，支持复杂路由规则；
Kafka适合高吞吐的日志收集和流式计算。
我们的场景是业务消息通知，RabbitMQ更合适，而且学习成本低。"

---

## 五、技术亮点总结（简历可写）

### 5.1 后端技术亮点
1. **微服务架构**：9个独立服务，按业务领域拆分，独立部署扩展
2. **分布式锁**：Redisson实现，解决高并发重复下单问题
3. **JWT双令牌**：Access Token + Refresh Token，网关统一鉴权
4. **消息队列**：RabbitMQ实现跨服务异步通信，解耦服务依赖
   - 订单状态变更 → 车辆服务（更新车辆状态）
   - 订单完成 → 用户服务（更新积分和会员等级）
   - 使用Direct Exchange + 路由键精确分发
5. **分布式事务**：Seata保证跨服务数据一致性
6. **流量防护**：Sentinel限流熔断，保障系统高可用

### 5.2 AI Agent技术亮点
1. **Agent架构**：意图识别 → 数据查询 → Prompt构建 → LLM生成
2. **规则引擎**：基于关键词和正则的意图识别，准确率90%+
3. **流式响应**：SSE实现打字机效果，提升用户体验
4. **数据注入**：实时查询业务数据注入Prompt，避免LLM幻觉
5. **多轮对话**：基于conversation_id的上下文管理
6. **跨服务集成**：通过网关调用后端微服务API，数据互通

---

## 六、面试话术模板

### 6.1 自我介绍
"你好，我是XXX，最近在做畅行租车项目，这是一个基于Spring Cloud微服务架构的汽车租赁平台。
我负责后端核心服务开发和智能客服AI Agent的实现。
后端涉及分布式锁、消息队列、JWT认证等技术；
AI Agent基于Python FastAPI和OpenAI API，实现了意图识别、流式响应、多轮对话等功能。"

### 6.2 项目介绍
"项目采用微服务架构，后端9个服务，前端Vue3，还有一个Python AI Agent。
核心功能包括用户认证、车辆管理、订单管理、门店管理、评价系统、智能客服。
技术亮点是用Redisson解决高并发重复下单，用RabbitMQ解耦服务（订单状态变更通知车辆服务，订单完成通知用户服务更新积分），用Agent实现智能客服。"

### 6.3 技术难点
"最大的难点是高并发下车辆重复下单问题。
解决方案是Redisson分布式锁，按车辆ID加锁，5秒等待，30秒有效期。
同时结合Redis缓存和数据库乐观锁，多层防护确保数据一致性。"

---

## 七、注意事项

1. **准备项目演示**：最好能本地启动项目，演示核心功能
2. **准备代码片段**：关键代码（分布式锁、JWT、Agent）要能手写
3. **准备架构图**：画清楚微服务架构、数据流向、部署架构
4. **准备量化数据**：如服务数量、QPS、响应时间、准确率等
5. **准备问题**：提前准备常见面试问题的答案
6. **诚实回答**：不懂的不要硬撑，可以说"这块我了解不深，但我知道..."

---

## 八、RabbitMQ代码文件清单

### 8.1 订单服务（消息生产者）
```
changxing-booking/
├── config/
│   └── RabbitMQConfig.java          # RabbitMQ配置（交换机、队列、绑定）
├── dto/
│   └── MessageDTO.java              # 消息数据模型
├── service/
│   ├── BookingService.java          # 订单服务（调用消息发送）
│   └── MessageProducerService.java  # 消息生产者服务
└── src/main/resources/
    └── application.yml              # 添加RabbitMQ配置
```

### 8.2 车辆服务（消息消费者）
```
changxing-car/
├── config/
│   └── RabbitMQConfig.java          # RabbitMQ配置
├── consumer/
│   └── BookingMessageConsumer.java  # 消费订单状态消息
└── src/main/resources/
    └── application.yml              # 添加RabbitMQ配置
```

### 8.3 用户服务（消息消费者）
```
changxing-user/
├── config/
│   └── RabbitMQConfig.java          # RabbitMQ配置
├── consumer/
│   └── BookingMessageConsumer.java  # 消费订单完成消息
├── service/
│   └── UserService.java             # 添加积分和会员等级方法
└── src/main/resources/
    └── application.yml              # 添加RabbitMQ配置
```

### 8.4 消息流转图
```
订单服务 (Producer)
    │
    ├── booking.status.queue ──→ 车辆服务 (Consumer)
    │   (订单状态变更)              (更新车辆状态)
    │
    └── booking.completed.queue ──→ 用户服务 (Consumer)
        (订单完成)                    (更新积分和会员等级)
```

### 8.5 面试时可以展示的代码片段
1. **RabbitMQConfig.java** - 交换机、队列、绑定配置
2. **MessageProducerService.java** - 消息发送逻辑
3. **BookingMessageConsumer.java** - 消息消费和状态映射逻辑
4. **application.yml** - RabbitMQ连接配置
