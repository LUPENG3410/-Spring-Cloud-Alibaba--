# 畅行租车后端 — 面试总结（精细版）

> 用一句话概括：一个基于 Spring Cloud Alibaba 的租车微服务，核心难点是「高峰期抢车」——同一辆车在重叠日期段不能超卖。我从**读**（车辆列表）、**写**（下单预占）、**削峰**（异步落库）、**数据层**（索引/归档/短事务）、**防护**（Sentinel）五个方向做了纵深优化，形成一套端到端的防护链路。

---

## 零、项目全景（自我介绍用）

- **规模**：后端 10 个微服务 + 1 个 Python AI 客服 Agent + Vue3 前端
- **技术栈**：Spring Boot 3.2 / Spring Cloud 2023 / Spring Cloud Alibaba / Nacos / OpenFeign / Gateway / Sentinel / MySQL 8.0 / Redis 7 / RabbitMQ / MyBatis-Plus / MinIO

| 服务 | 职责 |
|------|------|
| gateway(8080) | 统一入口：路由、鉴权(AuthFilter)、限流 |
| auth(9000) | 登录注册、JWT 双令牌 |
| user(9001) | 用户、会员等级、积分 |
| car(9002) | 车辆/品牌/车系/车款、列表/详情/热门 |
| booking(9003) | 订单、可用性检查、保险（核心） |
| store(9004) | 门店、地图 |
| review(9005) | 评价、评分 |
| admin(9006) | 后台管理、统计、维保提醒 |
| service(9008) | 在线客服会话（对接 AI Agent） |
| upload(9009) | 图片上传（MinIO） |

> 面试第一句就点出：**租车库存不是电商的 `stock-1` 计数，而是「时间段占用」**。同一辆车 6/15~6/18 被租，任何日期重叠的订单都不能再租。这句话就能把你和只会背"Redis 预扣库存"的人区分开。

---

## 一、整体防护链路（面试必答的主线，能画图讲）

一个「抢车下单」请求从头到尾经过 6 层防护，任何一层挡住就是快速失败，穿透到底还有兜底：

```
请求 → ① 网关层         JWT鉴权 + 路由 + 白名单
        ② 接口层         Sentinel：下单QPS限流 + 热门车款热点参数限流
        ③ 读缓存层       车辆列表：Caffeine(60s) → Redis(5min) → MySQL
        ④ 业务层(写)     Redis ZSET + Lua 原子日期段预占（防超卖）
        ⑤ 数据层         MQ异步落库 + 短事务 + 复合索引 + DB唯一下限兜底
        ⑥ 兜底层         定时对账重建占用 + 超时释放 + 冷热归档
```

- **② 之前**叫「限流」（对流量），**④ 之后**叫「防超卖」（对数据），中间用 MQ 削峰解耦。
- 面试话术：*"我把防护做成了纵深防御：流量在最外层被 Sentinel 拦住，真流量进来后，读走二级缓存不碰库，写走 Redis 原子预占不超卖，最终落库有唯一索引兜底，失配了还有定时对账把它拉回来。每一层都有退路。"*

---

## 二、精细到每个业务（问题 → 方案 → 收益 → 一句话）

### 2.1 车辆列表 / 热门车型（读多写少 → 二级缓存）

**问题**：`GET /cars` 每次都是 `car_trims JOIN cars JOIN stores` 三表关联 + `ORDER BY`；`GET /cars/hot` 还带 `LEFT JOIN bookings` 的 `COUNT` 聚合，首页高频访问直打 MySQL，是典型读热点。

**方案**（CarService + TwoLevelCache）：
1. Caffeine 本地缓存（容量 1000，TTL 60s）→ Redis（TTL 5min）→ MySQL 三级读取；
2. 空结果也缓存 30s（防穿透），过期时间加随机抖动（防雪崩）；
3. Cache Aside 一致性：更新车辆时**先更数据库、再删缓存**；
4. 启动时预热首页 + 热门车型，避免冷启动穿透。

**收益**：压测 `GET /cars` 5484 QPS、`GET /cars/hot` 7611 QPS，0 错误，P99 不到 60ms。

**一句话**：*"命中顺序本地→Redis→DB，本地砍掉 Redis 网络 IO，空值+随机过期防穿透雪崩，先更库后删缓存保一致。"*

**追问"本地缓存多实例不一致怎么办"**：车辆基础信息变更不频繁，60s 短暂不一致可接受；对强一致的字段（实时库存/车款详情含实时评价）**不进本地缓存**——要能主动说出"什么时候不用本地缓存"。

### 2.2 下单（写并发，核心）—— Redis 原子日期段预占替代分布式锁

**问题**：原来 `BookingService` 用 Redisson 锁 `booking:lock:car:{carId}` 排队。两个致命点：
1. 锁粒度是 `carId`，**可用性校验却是 `trimId + province + 日期段`**——锁和校验不在同一维度，不同日期/同款不同车被串行排队（吞吐低），反过来同款同省同日期不同 carId 又能绕过锁（竞态隐患）；
2. 锁等待导致请求串行，吞吐上不去。

**方案**（OccupancyService + Lua）：
- Redis ZSET 维护 `(trimId, province)` 维度的日期占用：
  ```
  key: occ:trim:{trimId}:{province}
  score = 订单 endDate 的 epochDay
  member = "startEpochDay~endEpochDay"
  ```
- Lua 原子完成「检查重叠 + 写入占用」：
  ```lua
  -- ZRANGEBYSCORE(key, start, '+inf') 取出所有 end>=start 的记录
  -- 再判 member 的 start' <= finish，有重叠就 return 0（失败），否则 ZADD 写占用 return 1
  ```
- 有重叠直接 `throw BusinessException("该车辆在该时间段已被预订")` 快速失败，不再排队等锁。

**收益**：不同车款、不同取车地天然并行；同一 `(trimId, province)` 的并发由 Lua 原子性保证，无需锁等待。

**一句话**：*"我不再用分布式锁排队，而是把日期段占用放进 Redis ZSET，用 Lua 原子地做'查重叠→写占用'，冲突就快速失败。租车是时间段资源，必须按区间判断，不能 DECR。"*

**追问 3 连**：
- *为什么不用 DECR？* 租车是时间段占用，不是剩余件数。
- *Redis 挂了/消息丢了？* DB 唯一索引是最后防线，定时对账用 MySQL 重建 Redis 占用。
- *和 Redisson 比？* 分布式锁是"串行等待"，Lua 预占是"原子判断 + 失败即返"，后者吞吐更高、不互相阻塞。

### 2.3 订单创建（削峰）—— MQ 异步落库

**问题**：落库 + 发状态消息都挡在请求线程，DB 写压力直击请求链路。

**方案**（BookingService.processBookingCreate）：
1. 请求线程只做：参数校验 → Redis 预占 → 生成订单号 → 发 `booking.create` 消息 → **立即返回"受理中"**；
2. 落库交给消费者异步做（幂等校验 `selectCount(orderNo)` → 事务外取价组装 → 短事务 insert → 发状态消息）；
3. 失败进死信队列重试。

**收益**：写 DB 的压力从请求线程剥离，实现削峰填谷。

**一句话**：*"下单受理和落库解耦，请求线程只做预占和发消息，立刻返回，DB 写入走 MQ 异步削峰，消费端做幂等。布"*

### 2.4 数据库层（索引 + 短事务 + 冷热分离 + 兜底）

**问题**：① 可用性重叠查询用 `BETWEEN OR BETWEEN OR (<= AND >=)`，范围 + OR 难走索引；② `@Transactional` 包住了 Feign 远程调用和 MQ 发送，事务周期长、行锁持有久；③ `bookings` 百万级持续增长，热点查询扫表范围大。

**方案**：
1. 复合索引（`sql/high_concurrency_optimize.sql`）：
   ```sql
   idx_trim_status_dates (trim_id, status, start_date, end_date)  -- 下单可用性检查
   idx_status_province_trim (status, province, trim_id)           -- 列表库存判断
   idx_trim_province_dates (trim_id, pickup_province, start_date, end_date) -- 防重兜底
   ```
2. 短事务（`BookingPersistService` 独立类，避免自调用 `@Transactional` 失效）：远程取价/组装在事务外，事务内只做「唯一性兜底校验 + insert」；
3. 冷热分离：`completed/cancelled` 订单每天凌晨归档到 `bookings_archive`，主表只留活跃订单；
4. DB 兜底：应用层预占之后，DB 唯一校验作为超卖最后一道防线。

**一句话**：*"远程调用全部移出事务，事务只包一个 insert，锁持有时间降到最短；再配合合索引让重叠查询走索引、历史订单归档缩表。"*

### 2.5 接口防护（Sentinel）

**问题**：抢热门车时流量全部压到 Redis 预占和 DB。

**方案**（SentinelRuleConfig + Controller 注解）：
- 下单接口 `createBooking`：整体 QPS 限流 100，超限 `blockHandler` 返回 429；
- 可用性检查 `checkAvailability`：热点参数限流（`paramIdx=0` 即 trimId），单个热门车款 QPS 20；
- 非核心链路（客服/积分）降级。

**压测实证**（这里要会讲数据）：
| 接口 | 实际 QPS | 限流阈值 | 表现 |
|------|---------|---------|------|
| POST 下单 | 852 | 100 | 99.96% 被拦（429 快速失败） |
| 可用性检查 | 1803 | 20（trimId=1） | 99.04% 被拦 |

**这个数据不是 bug，恰恰证明限流生效了**：852 QPS 打进来只放行 100，后端没被打垮。面试可以主动说："压测时 852 QPS 打进下单接口，Sentinel 把超出的拦在门外，这就是限流起作用的证据；要测真实吞吐我会先把阈值调高。"

**一句话**：*"热点参数限流解决'大家都在抢同一台车'——对同一个 trimId 单独限 QPS，超出直接失败，不把压力传导到预占和数据库。"*

**改进点**：规则现在是 `@PostConstruct` 写死的，最好改成从 Nacos 配置中心动态下发，生产收紧、压测放开。

### 2.6 数据一致性（兜底）

**方案**（OccupancyReconcileJob）：
1. 每分钟 `releaseTimeoutPending`：扫描超时未支付的 pending 订单 → 置 cancelled → 释放对应 Redis 占用；
2. 每 10 分钟 `rebuildFromDatabase`：以 MySQL 活跃订单为准全量重建 Redis 占用集合，清理孤儿 key。

**一句话**：*"Redis 占用和 MySQL 最终一致性靠定时对账保证：超时未支付自动释放，周期性地用数据库活跃订单重建 Redis，把 Redis 当可重建的缓存而非唯一事实源。"*

### 2.7 认证 / 网关

- JWT 双令牌（Access 2h + Refresh 7d）+ 网关 `AuthFilter` 统一鉴权，白名单放行，验证通过后注入 `X-User-Id` / `X-User-Role` 给下游；
- 登录目前 `UserService.login` 用 `user.getPassword().equals(password)` 明文比对——**这是安全隐患，面试若被问安全要主动承认并说会改 BCrypt**。

---

## 三、收益汇总（一张表）

| 场景 | 优化前 | 优化后 | 收益 |
|------|--------|--------|------|
| 车辆列表/热门 | 三表 JOIN 直打 DB | Caffeine+Redis 二级缓存+预热 | 5484/7611 QPS，0 错误 |
| 高峰期下单 | Redisson 锁按 carId 串行+长事务 | ZSET+Lua 原子预占+MQ 异步+短事务 | 吞吐提升，消除超卖与竞态 |
| 订单落库 | 同步写库挡请求线程 | 异步削峰 | DB 写压力剥离 |
| 数据库 | 难走索引+事务长+主表大 | 复合索引+短事务+归档 | 行锁、扫表范围下降 |
| 接口防护 | Sentinel 空壳 | QPS+热点参数限流 | 852 QPS 被拦到 100，核心稳定 |
| 一致性 | 依赖锁 | 定时对账+超时释放 | Redis/MySQL 最终一致 |

---

## 四、高频追问清单（建议全部脱稿）

1. 缓存穿透/雪崩/击穿怎么防？对应哪些代码？
2. 为什么「先更库、后删缓存」，而不是反过来？
3. ZSET + Lua 预占和 MySQL 最终一致性怎么保证？
4. 下单消息丢失怎么办？（持久化 + 生产者确认 + 死信）
5. 为什么租车不用 DECR 而用日期段占用？
6. 热点参数限流原理？（按参数值单独计数）
7. 本地缓存多实例不一致，你的取舍？
8. 事务为什么不能包远程调用和发消息？
9. Redisson 锁和 Redis Lua 预占的本质区别？

## 五、30 秒项目介绍模板

"我做过一个畅行租车项目，Spring Cloud Alibaba 微服务，后端 10 个服务 + Python AI 客服。我负责的核心是**高并发下单和车辆查询的性能优化**：读请求用 Caffeine+Redis 二级缓存，把车辆列表 QPS 打到 5400+；写请求用 Redis ZSET+Lua 做日期段原子预占替代分布式锁，加 MQ 异步落库削峰，短事务+复合索引+冷热归档优化数据库，Sentinel 做 QPS 和热点限流，最后用定时对账兜底一致性。整体是一套从网关到数据的纵深防护链路。"